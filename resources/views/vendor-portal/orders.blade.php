@extends('layouts.vendor-portal')
@section('title', 'Purchase Orders')
@section('content')
<div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-wider text-teal-700">Purchase orders</p><h1 class="mt-1 text-2xl font-bold text-slate-900">Your purchase orders</h1><p class="mt-1 text-sm text-slate-500">Invoice each item only up to its remaining approved quantity.</p></div><div class="flex gap-1 rounded-lg bg-slate-100 p-1 text-xs font-semibold"><a href="{{ route('vendor.portal.orders.index') }}" class="rounded-md px-3 py-2 {{ $filter === 'all' ? 'bg-white text-slate-900 shadow-sm' : 'text-slate-500' }}">All</a><a href="{{ route('vendor.portal.orders.index', ['filter' => 'open']) }}" class="rounded-md px-3 py-2 {{ $filter === 'open' ? 'bg-white text-slate-900 shadow-sm' : 'text-slate-500' }}">Open</a><a href="{{ route('vendor.portal.orders.index', ['filter' => 'completed']) }}" class="rounded-md px-3 py-2 {{ $filter === 'completed' ? 'bg-white text-slate-900 shadow-sm' : 'text-slate-500' }}">Completed</a></div></div>
<section class="mt-6 divide-y divide-slate-100 overflow-hidden rounded-xl border border-slate-200 bg-white">@forelse($displayOrders as $order)@php
    $complete = $order->allItemsInvoiced();
@endphp<div class="flex flex-col gap-4 px-5 py-5 sm:px-6 lg:flex-row lg:items-center lg:justify-between"><div><div class="flex flex-wrap items-center gap-2"><p class="font-mono text-sm font-bold">{{ $order->po_number }}</p><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $complete ? 'bg-emerald-100 text-emerald-800' : 'bg-amber-100 text-amber-800' }}">{{ $complete ? 'Invoice complete' : 'Invoice required' }}</span></div><p class="mt-2 text-sm text-slate-600">{{ $order->items->count() }} item{{ $order->items->count() === 1 ? '' : 's' }} · Delivery by {{ $order->expected_delivery_date?->format('d M Y') ?? 'Not specified' }}</p></div><div class="flex items-center justify-between gap-5"><p class="font-mono font-semibold">Rs {{ number_format($order->total_amount, 2) }}</p><a href="{{ route('vendor.portal.orders.show', $order) }}" class="rounded-lg px-4 py-2.5 text-sm font-semibold {{ $complete ? 'border border-slate-300 text-slate-700' : 'bg-teal-600 text-white hover:bg-teal-700' }}">{{ $complete ? 'View order' : 'Open & invoice' }}</a></div></div>@empty<div class="px-6 py-14 text-center text-sm text-slate-500">No purchase orders match this view.</div>@endforelse</section>
@endsection
